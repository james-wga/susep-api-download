services:
  - type: web
    name: susep-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
```

#### **5. Deploy no Render**
1. Volte ao **Render.com**
2. Clique em **"New +"** → **"Web Service"**
3. Conecte seu repositório do GitHub
4. Configure:
   - **Name:** `susep-api`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
5. Clique em **"Create Web Service"**

#### **6. Aguardar Deploy**
- Render vai instalar e rodar (5-10 minutos)
- Quando terminar, você terá uma URL tipo:
```
  https://susep-api.onrender.com
